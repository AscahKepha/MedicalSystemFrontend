{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://healthcareaura.azurewebsites.net/api/:path*",
      "allowedRoles": ["anonymous"]
    }
  ]
}